# 税务数据导出插件 · 安装说明

本插件是 **Chrome 浏览器扩展**（无需编译、无需安装 Node.js），解压后直接在浏览器中加载即可使用。

## 一、环境要求

- Chrome 浏览器（推荐 110 以上版本）；Edge 浏览器也可以（步骤相同）

## 二、安装步骤（Chrome）

1. 把收到的压缩包（文件名形如 `tax-export-extension-v1.0.1.zip`，版本号会随更新变化）**解压**到一个**固定位置**
   （例如 `D:\工具\tax-export\` 或 `~/工具/tax-export/`，安装后不要删除或移动该文件夹，否则插件会失效）。
2. 打开 Chrome，地址栏输入 `chrome://extensions` 回车，进入扩展管理页。
3. 打开右上角的 **「开发者模式」** 开关。
4. 点击左上角 **「加载已解压的扩展程序」**，选择解压出来的 `tax-export` 文件夹
   （就是里面含有 `manifest.json` 的那一层文件夹）。
5. 扩展列表出现「税务数据导出」，即安装成功。建议点击工具栏拼图图标 🧩，把本插件**固定**到工具栏。

### Edge 浏览器

打开 `edge://extensions` → 开启左下角/右侧「开发人员模式」→「加载解压缩的扩展」，其余相同。

## 三、使用方法

1. 登录电子税务局：<https://etax.chinatax.gov.cn/>
2. 点击工具栏中的插件图标，弹出控制台。
3. 点击 **「开始采集」**，之后全自动运行；中途页面刷新属于正常现象，插件会自动续采，无需操作。
4. 采集完成后浏览器自动下载 `税务数据报表_年月日_时分秒.pdf`；完整 JSON 保存在插件「历史」标签中，可随时重新导出 PDF。

## 四、常见问题

- **提示「未能加载扩展」**：确认选择的是解压后的文件夹本身，而不是 zip 压缩包；且该层文件夹内能看到 `manifest.json`。
- **插件图标消失/失效**：解压后的文件夹被移动或删除了。回到 `chrome://extensions` 点该插件的「重新加载」，或重新按上面步骤加载新位置。
- **升级新版本**：解压新压缩包覆盖旧文件夹（保持路径不变），到 `chrome://extensions` 点击该插件的 **刷新 ↻** 按钮即可，数据不丢。
- **卸载**：在 `chrome://extensions` 中移除该扩展，再删除解压的文件夹即可。

## 五、文件夹内容（技术说明）

```
tax-export/
├── manifest.json            # 扩展清单（MV3）
├── background.js            # 后台 service worker（进度管理、PDF 生成）
├── content/                 # 注入电子税务局页面的采集脚本
├── popup/                   # 工具栏弹窗控制台
├── report.html / report.js  # PDF 报表模板
├── icons/                   # 图标
└── vendor/pdf-lib.min.js    # PDF 生成库（已内置，无需联网安装）
```
